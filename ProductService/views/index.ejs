<%- include('layout/header') %>

<div class="container">
    <div class="row my-4">
        <div class="col-lg-12">
            <% if (message){ %>
                <div class="alert alert-dismissible fade show alert-<%=message.type%>" role="alert">
                    <button class="btn-close" type="button" data-bs-dismiss="alert" aria-label="Close"></button>
                    <strong><%= message.message %></strong>
                </div>
            <%} %>
            <div class="table-responsive">
                <% if (food != '') { %>
                    <table class="table table-striped text-center">
                        <thead>
                            <tr class="table-dark">
                                <th>ID</th>
                                <th>Image</th>
                                <th>Name</th>
                                <th>Type</th>
                                <th>Price</th>
                                <th>Discount</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% food.forEach((row, index) => { %>
                                <tr>
                                    <td><%= index %></td> <!-- Corrected: Added <%= %> around index -->
                                    <td><img src="<%= row.image %>" width="50" class="img-thumbnail"></td>
                                    <td><%= row.name %></td>
                                    <td><%= row.ofType %></td>
                                    <td><%= row.price %></td>
                                    <td><%= row.discount %></td>
                                    <td>
                                        <a href="/edit/<%= row._id %>" class="text-success"><i class="fas fa-edit fa-lg mx-1"></i></a>
                                        <a href="/edit/<%= row._id %>" class="text-danger"><i class="fas fa-trash fa-lg mx-1"></i></a>
                                    </td>
                                </tr>
                            <% }) %> <!-- Corrected: Added space before %>-->
                        </tbody>
                    </table>
                <% } else { %>
                    <h1 class="text-center text-secondary mt-5">No Food Currently Available</h1> <!-- Adjusted message -->
                <% } %>
                
            </div>
        </div>
    </div>
</div>

<%- include('layout/footer') %>


