name: Echo Date
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
    
jobs:
  Echo-Date-when-event-was-done:
    runs-on: ubuntu-latest
    steps:
      - name: 'Step 1'
        run: echo $pwd
        shell: bash
